<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Films Project: Войти</title>
        <link href="styles/styles.css" rel="stylesheet" ref="text/css">
        <link href="styles/background.css" rel="stylesheet" ref="text/css">
    </head>
    <body class="background-image">
        <div class="header">
            <h2>Films</h2>
        </div>
        <div class="main_content">
            <h2>Посты от наших пользователей</h2>
        {{if Model.Count == 0}}
            <h4 class="nothing">Здесь пока ничего нет, но ты можешь быть первым :)</h4>
        {{else}}
            {{for post in Model}}
            <div class="post" id="post_{{post.Id}}">
                <div class="user_name">{{post.AuthorName}}</div>
                <div>23.11.2022</div>
                <div>{{post.Content}}</div>
            </div>
            {{end}}
        {{end}}

            <form class="post_adder_div" action="/post/addpost" method="post">
                <textarea rows="4"></textarea>
                <input type="submit" class="submit_btn" value="Опубликовать">
            </form>
        </div>
        {{if Model.Count != 0}}
            <div class="footer"></div>
        {{end}}
    </body>
</html>